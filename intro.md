QQ 群表情包管理：回复图片即可入库、按关键词随机发送、按 MD5 精准删除，配套备份恢复与管理台。

## 添加与入库
- 添加{关键词} ─ 回复一条带图消息批量入库；同消息直接带图也认
- 批量添加合并转发{关键词} ─ 先发命令，90 秒内本人再发合并转发，自动提取其中图片批量入库
- 重复图片按 MD5 自动跳过，不会在同一关键词下存两遍

## 发送与查看
- 来只{关键词} ─ 随机发送该关键词下的一张图片
- 列表{关键词}{页码} ─ 分页查看文件名，每页 10 张（仅管理员）

## 管理与清理
- 删图 ─ 回复要删的图片，按 MD5 在所有关键词中定位并删除
- 删图{关键词} / 删图{关键词}{序号} ─ 限定范围或按序号删除（仅管理员）
- 删除{关键词} ─ 60 秒内再发一次即确认，删除整个关键词目录（仅管理员）
- 屏蔽{关键词} / 屏蔽列表 / 解除屏蔽{关键词} ─ 冻结关键词：禁止添加、发送与查看（仅管理员）
- 统计 ─ 关键词数、图片总数与图片数 Top 3（仅管理员）

## 主动行为
- 无定时推送；仅在收到指令时响应
- 存储超过上限时在管理台与统计里给出超容量警告

## 管理台
- WebUI「表情包管理」页 ─ 管理员设置、存储上限、一键备份（zip + MD5 校验清单）、恢复（合并式导入）、清空
- 菜单与渲染自带内置 Emoji 字体，服务器不装字体也能正常显示
