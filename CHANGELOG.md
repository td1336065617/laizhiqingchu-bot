# 更新日志

## [1.1.0] - 2026-08-14

### 新增
- `删图{关键词}` 支持**回复图片按 MD5 精准删除**：回复要删除的图片并发送
  `删图{关键词}`，即可删除该关键词下 MD5 匹配的图片（支持一次回复多张，
  同 MD5 的多份文件一并删除）；原 `删图{关键词}{序号}` 按序号删除保留。
- 统一的 **AstrBot WebUI 管理后台**（插件管理 → 表情包管理）：
  - 管理员列表与存储上限设置（添加/移除自动保存）；
  - 备份：一键打包全部表情包数据为 zip（含 `manifest.json` MD5 校验清单）；
  - 恢复：上传 zip 校验后合并导入，重叠图片按 MD5 跳过；
  - 清空：二次确认后删除全部表情包（不影响管理员、屏蔽列表与备份）。
- 关键词**屏蔽**功能：`屏蔽{关键词}`、`屏蔽列表`、`解除屏蔽{关键词}`。

### 变更
- 移除 AstrBot 原生插件配置表单（不再有 `_conf_schema.json`），
  配置由插件自管理，统一在 WebUI 页面操作。
- `来只{关键词}` 只回图片、不再附带“来只 xxx”文字。

### 修复
- 修复 WebUI 备份列表/配置读取的响应解析问题（管理员列表、存储上限、
  备份列表此前可能显示为空）。
- 修复插件 Web API 路由未带插件名前缀导致“未找到该路由”的问题。

## [1.0.0] - 2026-08-13

### 新增
- 基础表情包管理：`添加{关键词}`（回复图片或同消息带图）、
  `来只{关键词}`、`列表{关键词}{页码}`、`删图{关键词}{序号}`、
  `删除{关键词}`（60 秒二次确认）、`统计`、`菜单`。
- 支持群聊与私聊，指令精确前缀触发；关键词合法性校验与 MD5 去重。
- 存储容量检查（默认 200MB，超过记录警告日志）。
- 索引自修复：物理文件丢失时自动清理索引。
