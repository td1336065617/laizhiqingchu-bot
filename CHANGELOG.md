# 更新日志

## [1.1.4] - 2026-08-14

### 新增
- 新增专用命令 `批量添加合并转发{关键词}`：发送命令后 90 秒内，
  **命令发送者本人**发送的合并转发会自动提取全部图片批量入库
  （不再缓存所有合并转发消息，避免资源浪费与多个转发对错的问题）。

## [1.1.3] - 2026-08-14

### 新增
- `添加{关键词}` 支持 **QQ 合并转发（聊天记录）批量入库**：先在群里发送合并
  转发，然后回复该转发并发送 `添加{关键词}`，插件会自动提取转发内全部图片
  批量添加（嵌套消息与 JSON 内容均可解析，MD5 去重）。

## [1.1.2] - 2026-08-14

### 修复
- 屏蔽关键词现在真正“屏蔽”：被屏蔽的关键词禁止 `添加`、`来只` 发送和
  `列表` 查看（此前只禁止添加，容易误以为未生效）；`解除屏蔽` 后恢复。

## [1.1.1] - 2026-08-14

### 变更
- `删图` 回复图片删除不再强制要求带关键词：回复要删除的图片并发送 `删图`，
  即可按 MD5 自动在所有关键词中定位并删除（同一图片存在于多个关键词时一并删除）；
  `删图{关键词}` 仍可限定范围，`删图{关键词}{序号}` 按序号删除保持不变。

## [1.1.0] - 2026-08-14

### 新增
- `删图` 支持**回复图片按 MD5 精准删除**：回复要删除的图片并发送 `删图`，
  自动在所有关键词中定位并删除（支持一次回复多张，同 MD5 的多份文件一并
  删除；也可 `删图{关键词}` 限定在某个关键词下删除）；原
  `删图{关键词}{序号}` 按序号删除保留。
- 统一的 **AstrBot WebUI 管理后台**（插件管理 → 表情包管理）：
  - 管理员列表与存储上限设置（添加/移除自动保存）；
  - 备份：一键打包全部表情包数据为 zip（含 `manifest.json` MD5 校验清单）；
  - 恢复：上传 zip 校验后合并导入，重叠图片按 MD5 跳过；
  - 清空：二次确认后删除全部表情包（不影响管理员、屏蔽列表与备份）。
- 关键词**屏蔽**功能：`屏蔽{关键词}`、`屏蔽列表`、`解除屏蔽{关键词}`。

### 变更
- 移除 AstrBot 原生插件配置表单（不再有 `_conf_schema.json`），
  配置由插件自管理，统一在 WebUI 页面操作。
- `来只{关键词}` 只回图片、不再附带“来只 xxx”文字。

### 修复
- 修复 WebUI 备份列表/配置读取的响应解析问题（管理员列表、存储上限、
  备份列表此前可能显示为空）。
- 修复插件 Web API 路由未带插件名前缀导致“未找到该路由”的问题。

## [1.0.0] - 2026-08-13

### 新增
- 基础表情包管理：`添加{关键词}`（回复图片或同消息带图）、
  `来只{关键词}`、`列表{关键词}{页码}`、`删图{关键词}{序号}`、
  `删除{关键词}`（60 秒二次确认）、`统计`、`菜单`。
- 支持群聊与私聊，指令精确前缀触发；关键词合法性校验与 MD5 去重。
- 存储容量检查（默认 200MB，超过记录警告日志）。
- 索引自修复：物理文件丢失时自动清理索引。
