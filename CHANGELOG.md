# 更新日志

本文件记录所有值得注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

图例：✨ 新增 · 🐛 修复 · ⚙️ 变更 · ⚠️ 升级注意

---

## [1.2.0] - 2026-09-22

> QQ 双通道适配：支持 OneBot v11（aiocqhttp），官方族行为零回归。

### ✨ 新增
- 新增 platform_compat.py：合并转发双解析（官方 102 / OneBot get_forward_msg）。
- 管理员列表支持「平台ID:用户ID」精确限定，旧配置继续兼容。

### ⚙️ 变更
- support_platforms 增加 qq_official_webhook、aiocqhttp。

### 🧪 真机验证
- 已用 NapCat（OneBot v11）验证：回复图片添加、来只发送、合并转发批量添加（get_forward_msg）。

---

## [1.1.20] - 2026-09-18

> 📖 根目录提供 `intro.md`，可被「菜单导航」插件的 `插件介绍` 指令聚合展示。

### ✨ 新增
- 插件介绍接入：说明添加/入库、随机发送、关键词管理、精准删除、屏蔽与 WebUI 备份恢复等具体功能。

---

## [1.1.19] - 2026-09-07

> 没有可用渲染器时，回退的文字版菜单排版不再被切碎。

### ⚙️ 变更
- 文字版菜单按长度分片发送时保留缩进，并优先在行尾或空格处断开，排版尽量与原版一致。

---

## [1.1.18] - 2026-09-06

> 菜单里的 Emoji 不再显示成"方框里的 X"，也不需要服务器预装 Emoji 字体。

### 🐛 修复
- Emoji 不再交给中文字体绘制，避免服务器缺少 Emoji 字形时出现"方框里的 X"。
- 插件内置 Emoji 字体，图片渲染与文字回退都能使用，不再要求服务器安装字体或系统软件。
- 字体加载失败时使用安全符号兜底。

### ⚙️ 变更
- 菜单图片缓存格式升级，旧图片会在下次展示时自动重绘。

---

## [1.1.17] - 2026-09-05

> 中文菜单不再挤成一团。

### ⚙️ 变更
- 放宽字间距、行距与分区留白，菜单更易读。
- 图片渲染与文字回退的排版保持一致；缓存格式升级，旧图片自动重绘。

---

## [1.1.16] - 2026-09-05

> 修复菜单中文显示为方框或字形错乱。

### 🐛 修复
- 渲染菜单时显式使用简体中文字体面，不再误用日文字库面导致中文显示为方框或字形错乱。

### ⚙️ 变更
- 菜单图片缓存格式升级，旧字体缓存自动失效并重新生成。

---

## [1.1.15] - 2026-09-04

> `表情包管理菜单` 改为以图片形式展示。

### ⚙️ 变更
- 菜单优先渲染成图片后发送；渲染失败时回退为原版文字排版。
- 渲染器自动挑选：Chromium / Chrome、Firefox、`wkhtmltoimage` 或 Pillow，不再依赖 Google Chrome。

---

## [1.1.14] - 2026-09-04

> 菜单视觉风格统一。

### ⚙️ 变更
- 菜单与「菜单导航」收录的内容统一为一套统一的主题配色与装饰元素。
- 指令、权限与菜单导航的解析格式保持不变，调用方无需改动。

---

## [1.1.13] - 2026-08-20

> 兼容 QQ 官方指令面板自动添加的 `/` 前缀。

### 🐛 修复
- `/添加{关键词}`、`/来只{关键词}`、`/表情包管理菜单` 等带斜杠前缀的写法与无前缀写法行为一致。

---

## [1.1.12] - 2026-08-17

> 修复 WebUI 更新时可能 404。

### 🐛 修复
- 仓库地址显式指向 `master` 分支，不再依赖 GitHub API 查询默认分支，避免回退到 `main` 导致更新失败。
- `repo` 地址更新，WebUI 的插件更新不再查询 `api.github.com`。

---

## [1.1.11] - 2026-08-16

> 菜单排版细节优化。

### ⚙️ 变更
- 调整菜单的分区图标、命令分隔符与分隔线排版。

---

## [1.1.10] - 2026-08-16

> 菜单带上开源仓库地址。

### ⚙️ 变更
- 菜单末尾附带插件开源仓库链接。

---

## [1.1.9] - 2026-08-16

> 支持被「菜单导航」类插件聚合。

### ✨ 新增
- 新增 `menu.md`，供「菜单导航」类插件把本插件菜单聚合到统一的 `菜单` 指令下。

---

## [1.1.8] - 2026-08-16

> `菜单` 指令更名为 `表情包管理菜单`，不再和其他插件抢指令。

### ⚙️ 变更
- 菜单指令由 `菜单` 改为 `表情包管理菜单`，避免多插件共存时冲突；`菜单` 不再被本插件拦截。

### ⚠️ 升级注意
- 习惯发 `菜单` 的用户需要改用 `表情包管理菜单`。

---

## [1.1.7] - 2026-08-14

> 修复 WebUI 备份列表的"删除"按钮点了没反应。

### 🐛 修复
- 备份删除确认改为二次点击确认，与"清空表情包"一致，不再依赖插件页面 iframe 沙箱不支持的原生确认弹窗。

---

## [1.1.6] - 2026-08-14

> WebUI 备份列表可以删除备份文件了。

### ✨ 新增
- 备份列表新增"删除"：二次确认后删除指定的备份压缩包，仅删除备份文件，不影响现有表情包数据。

---

## [1.1.5] - 2026-08-14

> 修复合并转发"解析不到图片"。

### 🐛 修复
- 兼容 QQ 官方实际推送的合并转发结构，图片 URL 内嵌在渲染摘要文本中时也能提取入库，不再提示"未从合并转发中解析到内容 / 没有解析到图片"。
- 解析失败时输出原始消息结构摘要日志，便于排查平台推送的新结构。

---

## [1.1.4] - 2026-08-14

> 新增合并转发批量入库的专用命令。

### ✨ 新增
- 新增 `批量添加合并转发{关键词}`：发送命令后 90 秒内，**命令发送者本人**发送的合并转发会自动提取图片批量入库。
- 不再缓存所有合并转发消息，避免资源浪费，也避免多个转发互相串台。

---

## [1.1.3] - 2026-08-14

> `添加` 支持回复合并转发批量入库。

### ✨ 新增
- 回复一条合并转发（聊天记录）并发送 `添加{关键词}`，插件自动提取转发内的全部图片批量入库，MD5 去重。

---

## [1.1.2] - 2026-08-14

> 让"屏蔽关键词"真正生效。

### 🐛 修复
- 被屏蔽的关键词同时禁止添加、发送与查看（此前只禁止添加，容易误以为屏蔽未生效）；`解除屏蔽` 后恢复。

---

## [1.1.1] - 2026-08-14

> 不带关键词也能删图。

### ⚙️ 变更
- `删图` 不再强制带关键词：回复要删除的图片并发送 `删图`，即可按 MD5 在所有关键词中定位并删除；`删图{关键词}` 与 `删图{关键词}{序号}` 用法不变。

---

## [1.1.0] - 2026-08-14

> 回复图片按 MD5 精准删除，并上线统一的 WebUI 管理后台。

### ✨ 新增
- `删图` 支持**回复图片按 MD5 精准删除**：一次回复多张也可以，同一张图的多个副本会一并删除，也可用 `删图{关键词}` 限定范围。
- 统一的 **AstrBot WebUI 管理后台**（插件管理 → 表情包管理）：
  - 管理员列表与存储上限设置，添加 / 移除自动保存；
  - 备份：一键打包全部表情包数据为 zip，含 MD5 校验清单；
  - 恢复：上传 zip 校验后合并导入，重叠图片按 MD5 跳过；
  - 清空：二次确认后删除全部表情包，不影响管理员、屏蔽列表与备份。
- 关键词**屏蔽**功能：`屏蔽{关键词}`、`屏蔽列表`、`解除屏蔽{关键词}`。

### ⚙️ 变更
- 移除 AstrBot 原生插件配置表单（不再有 `_conf_schema.json`），配置统一在 WebUI 页面操作。
- `来只{关键词}` 只回图片，不再附带"来只 xxx"文字。

### 🐛 修复
- 修复 WebUI 读取管理员列表、存储上限与备份列表时可能显示为空的问题。
- 修复插件 Web API 路由缺少插件名前缀导致的"未找到该路由"。

### ⚠️ 升级注意
- 管理员与存储上限不再在 AstrBot 原生配置表单里设置，改到 WebUI 的「表情包管理」页维护。
- `来只{关键词}` 现在只回图片，不再附带"来只 xxx"文字；若有依赖该文字的机器人逻辑请一并调整。

---

## [1.0.0] - 2026-08-13

> 首个版本：关键词表情包仓库。

### ✨ 新增
- 基础表情包管理：`添加{关键词}`（回复图片或同消息带图）、`来只{关键词}`、`列表{关键词}{页码}`、`删图{关键词}{序号}`、`删除{关键词}`（60 秒二次确认）、`统计`、`菜单`。
- 支持群聊与私聊，指令精确前缀触发；关键词合法性校验与 MD5 去重。
- 存储容量检查（默认 200 MB，超过记录警告日志）。
- 索引自修复：物理文件丢失时自动清理索引中的失效条目。
